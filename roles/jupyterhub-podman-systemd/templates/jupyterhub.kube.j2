[Install]
WantedBy=default.target

[Kube]
# Point to the yaml file in the same directory
Yaml=jupyterhub.yaml

# Use the jupyterhub network
Network=jupyterhub.network

# Publish the jupyterhub ports
PublishPort={{ service_port }}:{{ service_port }}/{{ service_protocol }}